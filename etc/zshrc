NIX_PROFILE=$HOME/.nix-profile

# Powerline
POWERLINE_MODULES="venv,user,ssh,cwd,nix-shell,perms,git,jobs,exit,root"
POWERLINE_PRIORITY="root,user,host,ssh,perms,git-branch,git-status,cwd,hg,jobs,exit,cwd-path"

function powerline_precmd() {
  PS1="$($NIX_PROFILE/bin/powerline-go -jobs ${${(%):%j}:-0} -modules $POWERLINE_MODULES -priority $POWERLINE_PRIORITY -cwd-mode dironly -max-width 60)"
}

function install_powerline_precmd() {
  for s in "${precmd_functions[@]}"; do
    if [ "$s" = "powerline_precmd" ]; then
      return
    fi
  done
  precmd_functions+=(powerline_precmd)
}

if [ "$TERM" != "linux" ] && [ -f "$NIX_PROFILE/bin/powerline-go" ]; then
    install_powerline_precmd
fi

# Bat
export BAT_THEME="Solarized (dark)"

# GPG Config
# $TTY is maintained by zsh itself, so pinpad prompts work without forking tty(1).
export GPG_TTY=$TTY

# Restore Key Binding
bindkey -e

# Zsh Extension
[ -f $NIX_PROFILE/share/zsh-autosuggestions/zsh-autosuggestions.zsh ] && source $NIX_PROFILE/share/zsh-autosuggestions/zsh-autosuggestions.zsh
ZSH_AUTOSUGGEST_HIGHLIGHT_STYLE="fg=10"
ZSH_AUTOSUGGEST_STRATEGY=(history completion)

# LaTex
export PATH="/Library/TeX/texbin:$PATH"

# Obisidian
[ -d "/Applications/Obsidian.app/Contents/MacOS" ] && export PATH="$PATH:/Applications/Obsidian.app/Contents/MacOS"

# LMStudio
[ -d "$HOME/.lmstudio/bin" ] && export PATH="$PATH:$HOME/.lmstudio/bin"

# Development
[ ! -d $HOME/.pnpm ] && mkdir -p $HOME/.pnpm
export PNPM_HOME=$HOME/.pnpm

export PATH=$NIX_PROFILE/bin:$PATH # Workaround for Nix
[ $(command -v rbenv) ] && eval "$(rbenv init -)"
[ $(command -v go) ] && export PATH=$PATH:$(go env GOPATH)/bin
[ -d $HOME/.npm-global ] && export PATH=$HOME/.npm-global/bin:$PATH # yarn will use this
[ $(command -v pnpm) ] && export PATH=$PATH:$PNPM_HOME
[ $(command -v cargo) ] && export PATH=$HOME/.cargo/bin:$PATH
[ -d $HOME/.local/bin ] && export PATH=$HOME/.local/bin:$PATH

# Native build dependencies come from both prefixes. Tie each variable to a zsh
# array marked unique, so a nested shell re-prepending the same entries cannot
# grow the value without bound.
typeset -TUx LIBRARY_PATH library_path :
typeset -TUx PKG_CONFIG_PATH pkg_config_path :
[ -d /opt/homebrew/lib ] && library_path=(/opt/homebrew/lib $library_path)
[ -d /opt/homebrew/lib/pkgconfig ] && pkg_config_path=(/opt/homebrew/lib/pkgconfig $pkg_config_path)
[ -d $NIX_PROFILE/lib/pkgconfig ] && pkg_config_path=($NIX_PROFILE/lib/pkgconfig $pkg_config_path)

# Godot
[ -f $HOME/Library/Application\ Support/Steam/steamapps/common/Godot\ Engine/Godot.app/Contents/MacOS/Godot ] && alias steam-godot="$HOME/Library/Application\ Support/Steam/steamapps/common/Godot\ Engine/Godot.app/Contents/MacOS/Godot"
[ -f $HOME/Library/Application\ Support/Steam/steamapps/common/Godot\ Engine/Godot.app/Contents/MacOS/Godot ] && alias steam-gut='steam-godot --debug-collisions --path $PWD -d -s addons/gut/gut_cmdln.gd'

# Nix
# nix-direnv's direnvrc is read by direnv, not by the shell -- it is linked into
# ~/.config/direnv/direnvrc instead (see README).
[ -f $NIX_PROFILE/bin/direnv ] && eval "$(direnv hook zsh)"

# Auto Complection
fpath=($NIX_PROFILE/share/zsh/site-functions $fpath)
[ -f $NIX_PROFILE/share/git/contrib/completion/git-completion.zsh ] && zstyle ':completion:*:*:git:*' script $NIX_PROFILE/share/git/contrib/completion/git-completion.zsh

autoload -Uz compinit && compinit
autoload -Uz add-zsh-hook

# Rename tmux window to current directory
if [ -n "$TMUX" ]; then
  function tmux_rename_window() {
     local window_id=$(tmux display-message -t "$TMUX_PANE" -p '#{window_id}')
     [ "$(tmux show-option -wqv -t "$window_id" @renamed)" = "1" ] && return
     local dir=$(git rev-parse --show-toplevel 2>/dev/null || echo "$PWD")
     local window_name=$(basename "$dir")
     tmux rename-window -t "$window_id" "$window_name"
  }

  typeset -g _tmux_ssh_active=

  function tmux_ssh_rename() {
     [[ "$1" =~ '^ssh([[:space:]]|$)' ]] || return
     local window_id=$(tmux display-message -t "$TMUX_PANE" -p '#{window_id}')
     [ "$(tmux show-option -wqv -t "$window_id" @renamed)" = "1" ] && return
     local host=${${(z)1}[-1]}
     host=${host##*@}
     tmux rename-window -t "$window_id" "$host"
     _tmux_ssh_active=1
  }

  function tmux_ssh_restore() {
     [ -n "$_tmux_ssh_active" ] || return
     _tmux_ssh_active=
     tmux_rename_window
  }

  add-zsh-hook chpwd tmux_rename_window
  add-zsh-hook preexec tmux_ssh_rename
  add-zsh-hook precmd tmux_ssh_restore

  tmux_rename_window
fi

# History
setopt HIST_FIND_NO_DUPS
setopt INC_APPEND_HISTORY

# Alias
alias be="bundle exec"
alias rake='noglob rake'

export EDITOR=vim

# Devbox as package manager
[ $(command -v devbox) ] && eval "$(devbox global shellenv --init-hook)"
